---
permalink: deploy/ontap-tools-deployment.html 
sidebar: sidebar 
keywords:  
summary: ONTAP tools for VMware vSphere部署为小型单节点，并包含核心服务以支持 NFS 和 VMFS 数据存储库。ONTAP 
---
= ONTAP tools for VMware vSphere
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
ONTAP tools for VMware vSphere部署为小型单节点，并包含核心服务以支持 NFS 和 VMFS 数据存储库。ONTAPONTAP部署过程最多可能需要 45 分钟。

.开始之前
VMware 中的内容库是一个容器对象，用于存储虚拟机模板、vApp 模板和其他类型的文件。使用内容库进行部署可为您提供无缝体验，因为它不依赖于网络连接。


NOTE: 您应该将内容库存储在共享数据存储上，以便集群内的所有主机都可以访问它。在将设备配置为 HA 配置之前，创建一个内容库来存储 OVA。部署后请勿删除内容库模板。


NOTE: 为了稍后启用 HA 部署，请不要直接在 ESXi 主机上部署托管ONTAP工具的虚拟机。将其部署在集群或资源池上。

如果您没有内容库，请按照以下步骤创建一个：

*创建内容库* 如果您计划仅使用小型单节点部署，则无需创建内容库。

. 从下载包含ONTAP tools for VMware vSphere的二进制文件 (_.ova_) 和签名证书的文件 https://mysupport.netapp.com/site/products/all/details/otv10/downloads-tab["NetApp 支持站点"^] 。
. 登录 vSphere 客户端
. 选择 vSphere 客户端菜单并选择*内容库*。
. 选择页面右侧的*创建*。
. 为库提供一个名称并创建内容库。
. 导航到您创建的内容库。
. 选择页面右侧的*操作*，选择*导入项目*并导入OVA文件。



NOTE: 更多信息，请参阅 https://blogs.vmware.com/vsphere/2020/01/creating-and-using-content-library.html["创建和使用内容库"]博客。


NOTE: 在继续部署之前，请将清单上的集群分布式资源调度程序 (DRS) 设置为“保守”。这可确保虚拟机在安装期间不会被迁移。

ONTAP tools for VMware vSphere最初部署为非 HA 设置。要扩展到 HA 部署，您需要启用 CPU 热插拔和内存热插件。您可以将此步骤作为部署过程的一部分执行，也可以在部署后编辑 VM 设置。

.步骤
. 从下载包含ONTAP tools for VMware vSphere的二进制文件 (_.ova_) 和签名证书的文件 https://mysupport.netapp.com/site/products/all/details/otv10/downloads-tab["NetApp 支持站点"^] 。如果您已经将OVA导入内容库，则可以跳过此步骤，继续下一步。
. 登录到 vSphere 服务器。
. 导航到您打算部署 OVA 的资源池、集群或主机。
+

NOTE: 切勿将ONTAP tools for VMware vSphere存储在其管理的vVols数据存储上。

. 您可以从内容库或本地系统部署 OVA。
+
|===


| 从本地系统 | 来自内容库 


| a. 右键单击并选择 *部署 OVF 模板...*。b. 从 URL 中选择 OVA 文件或浏览到其位置，然后选择 *下一步*。 | a. 转到您的内容库并选择要部署的库项目。b. 选择“操作”>“从此模板新建虚拟机” 
|===
. 在*选择名称和文件夹*字段中，输入虚拟机名称并选择其位置。
+
** 如果您使用的是 vCenter Server 8.0.3 版本，请选择选项 *自定义此虚拟机的硬件*，这将在进入 *准备完成* 窗口之前激活一个名为 *自定义硬件* 的附加步骤。
** 如果您使用的是 vCenter Server 7.0.3 版本，请按照部署结束时的*下一步是什么？*部分中的步骤进行操作。image:../media/select-name.png["选择名称和文件夹"]


. 选择计算机资源并选择*下一步*。或者，选中“*自动启动已部署的虚拟机*”复选框。
. 查看模板的详细信息并选择*下一步*。
. 阅读并接受许可协议，然后选择*下一步*。
. 选择配置的存储和磁盘格式，然后选择*下一步*。
. 为每个源网络选择目标网络，然后选择*下一步*。
. 在*自定义模板*窗口中，填写必填字段并选择*下一步*。image:../media/customize-temp.png["自定义模板"]
+
** 该信息在安装期间进行验证。如果存在差异，Web 控制台上会出现一条错误消息，并提示您进行更正。
** 主机名必须包含字母（AZ、az）、数字（0-9）和连字符（-）。要配置双栈，请指定映射到 IPv6 地址的主机名。
+

NOTE: 不支持纯 IPv6。包含 IPv6 和 IPv4 地址的 VLAN 支持混合模式。

** ONTAP工具 IP 地址是与ONTAP工具通信的主要接口。
** IPv4 是节点配置的 IP 地址组件，可用于在节点上启用诊断 shell 和 SSH 访问，以进行调试和维护。


. 使用 vCenter Server 8.0.3 版本时，在 *自定义硬件* 窗口中，启用 *CPU 热添加* 和 *内存热插拔* 选项以允许 HA 功能。image:../media/customize-hw.png["定制硬件"]
. 查看“准备完成”窗口中的详细信息，选择“完成”。
+
随着部署任务的创建，进度将显示在 vSphere 任务栏中。

. 如果未选择自动启动虚拟机的选项，则完成任务后启动虚拟机。


您可以在 VM 的 Web 控制台中跟踪安装进度。

如果 OVF 表格存在差异，则会出现一个对话框提示采取纠正措施。使用标签按钮进行导航，进行必要的更改，然后选择*确定*。您有三次机会来解决任何问题。如果三次尝试后问题仍然存在，安装过程将停止，建议在新的虚拟机上重新尝试安装。

.下一步是什么？
如果您已使用 vCenter Server 7.0.3 为ONTAP tools for VMware vSphere，请在部署后执行以下步骤。

. 登录 vCenter 客户端
. 关闭ONTAP工具节点。
. 导航到 *Inventories* 下的ONTAP tools for VMware vSphere并选择 *Edit settings* 选项。
. 在 *CPU* 选项下，选中 *启用 CPU 热添加* 复选框
. 在*内存*选项下，选中*内存热插拔*对应的*启用*复选框。

